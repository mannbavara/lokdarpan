# Hoist ESLint-related packages to prevent plugin conflicts
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*react-app*
# Use hoisted node linker to prevent duplicate plugin resolution
node-linker=hoisted





